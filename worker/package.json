{
  "name": "photovault-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "tsc -p tsconfig.test.json && node --test src/semantic.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "latest",
    "typescript": "latest"
  }
}
